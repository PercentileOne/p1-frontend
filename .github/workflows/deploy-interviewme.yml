name: Deploy interviewme.global → Azure SWA

on:
  push:
    branches: [main]
    paths:
      - 'src/viewme/public/**'
  workflow_dispatch:

jobs:
  build_and_deploy_job:
    runs-on: ubuntu-latest
    name: Build and Deploy

    steps:
      - uses: actions/checkout@v4

      - name: Prepare static output
        run: |
          mkdir -p deploy
          cp src/viewme/public/gate.html deploy/index.html
          cp src/viewme/public/mastermind-chair.png deploy/mastermind-chair.png
          cp src/viewme/public/interview-preview.png deploy/interview-preview.png
          [ -f src/viewme/public/candidate-1.jpg ] && cp src/viewme/public/candidate-1.jpg deploy/candidate-1.jpg || true
          [ -f src/viewme/public/candidate-2.jpg ] && cp src/viewme/public/candidate-2.jpg deploy/candidate-2.jpg || true
          [ -f src/viewme/public/candidate-3.jpg ] && cp src/viewme/public/candidate-3.jpg deploy/candidate-3.jpg || true
          [ -f src/viewme/public/candidate-4.jpg ] && cp src/viewme/public/candidate-4.jpg deploy/candidate-4.jpg || true
          cp src/viewme/public/staticwebapp.config.json deploy/staticwebapp.config.json

      - name: Deploy to Azure SWA
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_INTERVIEWME }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: deploy
          skip_app_build: true
