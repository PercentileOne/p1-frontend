import { UserList } from '../components/UserList'

export default function Employers() {
  return <UserList role="employer" title="Employers" entityLabel="Employer" searchPlaceholder="Search by name or email…" />
}
